<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://nsd9696.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://nsd9696.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-03-10T04:54:24+00:00</updated><id>https://nsd9696.github.io/feed.xml</id><title type="html">blank</title><subtitle>A simple, whitespace theme for academics. Based on [*folio](https://github.com/bogoli/-folio) design. </subtitle><entry xml:lang="en"><title type="html">Cross-Instance KV Cache Sharing for Disaggregated LLM Serving: Cutting TTFT with Mooncake and LMCache</title><link href="https://nsd9696.github.io/blog/2026/cross-instance-kv-cache-sharing-mooncake-lmcache/" rel="alternate" type="text/html" title="Cross-Instance KV Cache Sharing for Disaggregated LLM Serving: Cutting TTFT with Mooncake and LMCache"/><published>2026-03-09T00:00:00+00:00</published><updated>2026-03-09T00:00:00+00:00</updated><id>https://nsd9696.github.io/blog/2026/cross-instance-kv-cache-sharing-mooncake-lmcache</id><content type="html" xml:base="https://nsd9696.github.io/blog/2026/cross-instance-kv-cache-sharing-mooncake-lmcache/"><![CDATA[<h2 id="tldr">TL;DR</h2> <p>Disaggregated prefill-decode serving splits LLM inference across multiple instances, but each prefill instance maintains its own isolated KV cache. When the same prompt lands on a different instance, expensive prefill computation repeats from scratch. We implemented <strong>cross-instance KV cache sharing</strong> using Mooncake + LMCache on a multiple prefill-decode vLLM deployment, reducing cross-instance TTFT by <strong>24%</strong> compared to full recomputation.</p> <hr/> <h2 id="1-the-problem-isolated-kv-caches-in-multi-instance-serving">1. The Problem: Isolated KV Caches in Multi-Instance Serving</h2> <p>Modern LLM serving at scale requires multiple prefill instances to handle concurrent requests. vLLM’s <strong>Automatic Prefix Caching (APC)</strong> efficiently caches KV tensors within a single instance’s GPU memory — when the same system prompt arrives again at the same instance, prefill computation is skipped entirely.</p> <p>But what happens when the <strong>same prompt arrives at a different instance</strong>?</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Instance A: "You are an expert in..." → computes KV cache → stores in GPU A
Instance B: "You are an expert in..." → GPU B has nothing → recomputes from scratch
</code></pre></div></div> <p>In a multi-prefill setup, assuming round-robin routing (a load balancing strategy that distributes incoming requests evenly across instances in sequential order), there is a <strong>(N-1)/N chance</strong> that a repeated prompt lands on an instance that doesn’t have its KV cache. This means:</p> <ul> <li><strong>Wasted GPU compute</strong>: The same prefill computation runs redundantly across instances.</li> <li><strong>Higher TTFT</strong>: Users experience first-token latency equivalent to a cold request, even for prompts the system has seen before.</li> <li><strong>Poor scaling</strong>: Adding more prefill instances actually <em>reduces</em> per-instance cache hit rate.</li> </ul> <p>The core question: <strong>Can we share KV caches across instances so that any instance can serve a cached prompt?</strong></p> <h2 id="2-what-is-cross-instance-kv-cache-sharing">2. What is Cross-Instance KV Cache Sharing?</h2> <p>Cross-instance KV cache sharing creates a <strong>distributed KV cache pool</strong> that spans prefill (and optionally decode) instances. When one instance computes KV tensors for a prompt, those tensors are stored in a shared pool. When another instance receives the same prompt, it fetches pre-computed KV tensors from the pool instead of recomputing them.</p> <p>The cache hierarchy becomes:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Request arrives at Prefill Instance
  1. Check local GPU cache (APC) → fastest (~0.33s)
  2. Check distributed cache pool → fast (~0.60s)
  3. Full recomputation            → slowest (~0.81s)
</code></pre></div></div> <p>This is analogous to how CPU caches work: L1 (local APC) is fastest but per-core, while L2/L3 (distributed pool) is shared across cores with slightly higher latency.</p> <h2 id="3-our-solution-mooncake--lmcache-on-vllm">3. Our Solution: Mooncake + LMCache on vLLM</h2> <h3 id="architecture-overview">Architecture Overview</h3> <p>We deployed a <strong>disaggregated serving</strong> setup:</p> <ul> <li>Multiple prefill instances + multiple decode instances across several nodes</li> <li>Each instance with TP=2 (tensor parallelism)</li> <li>16 A100 80GB GPUs total</li> <li>NIXL + UCX for prefill-to-decode KV transfer</li> </ul> <p>For cross-instance KV cache sharing, we used vLLM’s <strong>MultiConnector</strong>:</p> <div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"kv_connector"</span><span class="p">:</span><span class="w"> </span><span class="s2">"MultiConnector"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"kv_role"</span><span class="p">:</span><span class="w"> </span><span class="s2">"kv_both"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"kv_connector_extra_config"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w">
    </span><span class="nl">"connectors"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="w">
      </span><span class="p">{</span><span class="w"> </span><span class="nl">"kv_connector"</span><span class="p">:</span><span class="w"> </span><span class="s2">"LMCacheConnectorV1"</span><span class="p">,</span><span class="w"> </span><span class="nl">"kv_role"</span><span class="p">:</span><span class="w"> </span><span class="s2">"kv_both"</span><span class="w"> </span><span class="p">},</span><span class="w">
      </span><span class="p">{</span><span class="w"> </span><span class="nl">"kv_connector"</span><span class="p">:</span><span class="w"> </span><span class="s2">"NixlConnector"</span><span class="p">,</span><span class="w"> </span><span class="nl">"kv_role"</span><span class="p">:</span><span class="w"> </span><span class="s2">"kv_both"</span><span class="w"> </span><span class="p">}</span><span class="w">
    </span><span class="p">]</span><span class="w">
  </span><span class="p">}</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div> <ul> <li><strong>LMCacheConnectorV1</strong>: Handles cross-instance prefix caching via Mooncake distributed store</li> <li><strong>NixlConnector</strong>: Handles prefill-to-decode GPU-to-GPU KV transfer via NIXL/UCX</li> </ul> <h3 id="how-multiconnector-works-internally">How MultiConnector Works Internally</h3> <p>On each request, MultiConnector iterates connectors in config order and calls <code class="language-plaintext highlighter-rouge">get_num_new_matched_tokens()</code> on each. The <strong>first connector that reports a cache hit</strong> is assigned to load KV for that request; all connectors are written to on save.</p> <p>In our setup, this means LMCacheConnectorV1 (Mooncake) is checked first, and NixlConnector handles prefill-to-decode transfer only when there is no Mooncake hit.</p> <h3 id="what-is-mooncake">What is Mooncake?</h3> <p><a href="https://github.com/kvcache-ai/Mooncake">Mooncake</a> (mooncake-transfer-engine) is a distributed KV cache pool originally developed by Moonshot AI. It provides:</p> <ul> <li><strong>Distributed memory pool</strong>: Aggregates memory across all connected workers</li> <li><strong>Metadata server</strong>: Tracks which KV chunks exist and where they are stored</li> <li><strong>RPC-based coordination</strong>: A master process coordinates chunk registration, lookup, and transfer</li> <li><strong>Chunk-level storage</strong>: KV tensors are split into fixed-size chunks (we used 64 tokens per chunk) for granular caching</li> </ul> <h3 id="how-lmcache-integrates-with-mooncake">How LMCache Integrates with Mooncake</h3> <p><a href="https://github.com/LMCache/LMCache">LMCache</a> is the caching layer that bridges vLLM and Mooncake. The data flow:</p> <ol> <li><strong>On prefill completion</strong>: LMCache stores computed KV chunks to Mooncake via <code class="language-plaintext highlighter-rouge">save_kv_layer</code> (Batch Put API)</li> <li><strong>On new request</strong>: vLLM first checks local APC. For tokens not in local APC, LMCache checks Mooncake (Exist + Batch Get API)</li> <li><strong>On cache hit</strong>: KV tensors are loaded from Mooncake directly into GPU memory, skipping prefill computation</li> </ol> <p>LMCache configuration for Mooncake:</p> <div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">local_cpu</span><span class="pi">:</span> <span class="s">False</span>
<span class="na">remote_url</span><span class="pi">:</span> <span class="s2">"</span><span class="s">mooncakestore://&lt;master-ip&gt;:&lt;rpc-port&gt;/"</span>
<span class="na">chunk_size</span><span class="pi">:</span> <span class="m">256</span> <span class="c1"># default: 256 tokens per chunk</span>
<span class="na">global_segment_size</span><span class="pi">:</span> <span class="m">3355443200</span> <span class="c1"># default: ~3.125 GB per worker</span>
<span class="na">protocol</span><span class="pi">:</span> <span class="s2">"</span><span class="s">tcp"</span> <span class="c1"># default: "tcp"</span>
</code></pre></div></div> <p>Key parameters:</p> <ul> <li><strong><code class="language-plaintext highlighter-rouge">remote_url</code></strong>: Address of the Mooncake master. The <code class="language-plaintext highlighter-rouge">mooncakestore://</code> scheme tells LMCache to use Mooncake as the distributed backend.</li> <li><strong><code class="language-plaintext highlighter-rouge">chunk_size</code></strong>: Number of tokens per KV cache chunk. Smaller values allow finer-grained cache reuse — shorter prompts can be partially served from cache.</li> <li><strong><code class="language-plaintext highlighter-rouge">global_segment_size</code></strong>: Amount of memory (in bytes) allocated per worker in the Mooncake pool.</li> <li><strong><code class="language-plaintext highlighter-rouge">protocol</code></strong>: Transport protocol used for chunk transfer between workers (<code class="language-plaintext highlighter-rouge">tcp</code> or <code class="language-plaintext highlighter-rouge">rdma</code>).</li> </ul> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/blog_mooncacke_1-480.webp 480w,/assets/img/blog_mooncacke_1-800.webp 800w,/assets/img/blog_mooncacke_1-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/blog_mooncacke_1.png" class="img-fluid rounded z-depth-1 d-block mx-auto" width="80%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Cross-Instance KV Cache Sharing architecture with LMCache + Mooncake. Orange path shows cold request flow (full prefill compute), blue dashed path shows cross-instance cache hit flow (KV fetched from Mooncake), and green path shows prefill-to-decode transfer via NixlConnector.</figcaption> </figure> <h2 id="4-cache-aware-routing">4. Cache-Aware Routing</h2> <p>Cross-instance caching alone isn’t enough — <strong>routing strategy</strong> determines how often cache hits actually occur.</p> <h3 id="vllm-router">vllm-router</h3> <p><a href="https://github.com/vllm-project/router">vllm-router</a> is a lightweight, high-performance router built in Rust, developed by the vLLM team. It supports several routing policies:</p> <ul> <li><strong>consistent_hash</strong>: Hashes the prompt and routes to a fixed instance. Same prompt always goes to the same instance → local APC handles it → Mooncake is never needed.</li> <li><strong>round_robin</strong>: Distributes requests evenly. Same prompt goes to different instances → Mooncake helps as fallback.</li> <li><strong>power_of_two</strong>: Randomly picks two candidate instances and routes to the one with the shorter queue — low overhead with good load distribution.</li> </ul> <p>vllm-router also natively supports P/D disaggregation, allowing explicit routing of prefill and decode requests to separate instance pools.</p> <p><strong>Pros</strong>: Extremely lightweight (Rust-based, minimal overhead), simple to deploy, native P/D disaggregation support, and benchmarks show 25% higher throughput than llm-d in certain workloads.</p> <p><strong>Cons</strong>: Routing decisions are stateless with respect to cache — <code class="language-plaintext highlighter-rouge">consistent_hash</code> maximizes local APC hits but can cause uneven load, while <code class="language-plaintext highlighter-rouge">round_robin</code> balances load but increases cross-instance misses. No real-time visibility into actual cache state across instances.</p> <h3 id="llm-d">llm-d</h3> <p><a href="https://github.com/llm-d/llm-d">llm-d</a> is a Kubernetes-native inference framework that implements <strong>precise prefix-cache aware scheduling</strong> via its External Processing Pod (EPP). vLLM pods publish cache block events (BlockStored, BlockRemoved) via ZMQ, which llm-d aggregates into a global, near-real-time <code class="language-plaintext highlighter-rouge">kvblock.Index</code>. The EPP’s <code class="language-plaintext highlighter-rouge">prefix-cache-scorer</code> queries this index on every incoming request to route to the instance most likely to have a cache hit:</p> <div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># Example: llm-d's Endpoint Picker configuration</span>
<span class="na">schedulingProfiles</span><span class="pi">:</span>
  <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">prefill</span>
    <span class="na">plugins</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="na">pluginRef</span><span class="pi">:</span> <span class="s">queue-scorer</span> <span class="c1"># prefer instances with shorter queues</span>
        <span class="na">weight</span><span class="pi">:</span> <span class="m">1.0</span>
      <span class="pi">-</span> <span class="na">pluginRef</span><span class="pi">:</span> <span class="s">prefix-cache-scorer</span> <span class="c1"># prefer instances with actual cached blocks</span>
        <span class="na">weight</span><span class="pi">:</span> <span class="m">1.0</span>
</code></pre></div></div> <p><strong>Pros</strong>: Makes routing decisions based on actual real-time cache state across the cluster, not just heuristics. Particularly effective for workloads with shared long prefixes (e.g., RAG, multi-turn conversations). Maximizes local APC hit rate, reducing the need for cross-instance Mooncake fallback.</p> <p><strong>Cons</strong>: More complex to operate (requires ZMQ event pipeline, in-memory index, Kubernetes-native deployment). Adds scheduling overhead — benchmarks show higher TTFT than vllm-router in some configurations due to the scoring computation.</p> <h3 id="comparison">Comparison</h3> <table> <thead> <tr> <th> </th> <th><strong>vllm-router</strong></th> <th><strong>llm-d</strong></th> </tr> </thead> <tbody> <tr> <td>Cache awareness</td> <td>Heuristic (hash/queue-based)</td> <td>Precise (real-time block index)</td> </tr> <tr> <td>Deployment</td> <td>Lightweight, any environment</td> <td>Kubernetes-native</td> </tr> <tr> <td>P/D disaggregation</td> <td>Native support</td> <td>Supported</td> </tr> <tr> <td>Operational complexity</td> <td>Low</td> <td>High</td> </tr> <tr> <td>Best for</td> <td>General serving, high throughput</td> <td>Workloads with shared long prefixes</td> </tr> </tbody> </table> <p>The ideal architecture combines cache-aware routing (to maximize local APC hits) with cross-instance sharing via Mooncake (as a fallback when requests inevitably land on non-optimal instances).</p> <h2 id="5-related-work">5. Related Work</h2> <h3 id="lmcache--mooncake-tencent--lmcache-team">LMCache + Mooncake (Tencent / LMCache Team)</h3> <p>The <a href="https://blog.lmcache.ai/2025-05-08-mooncake/">LMCache team’s blog</a> describes integrating Mooncake as a distributed backend for cross-process KV sharing. Their setup demonstrated KV cache sharing across multiple vLLM instances, validating the architecture we adopted.</p> <h3 id="llm-d-disaggregated-pd-with-kv-cache-sharing">llm-d Disaggregated PD with KV Cache Sharing</h3> <p>The <a href="https://llm-d.ai/blog/kvcache-wins-you-can-see">llm-d KV Cache blog</a> demonstrates significant latency improvements through cache-aware scheduling in disaggregated prefill-decode setups. Their EPP router with prefix-cache-scorer achieves high cache hit rates by intelligently routing requests to cache-warm instances.</p> <h3 id="vllm-production-stack">vLLM Production Stack</h3> <p>The <a href="https://docs.vllm.ai/projects/production-stack/en/latest/use_cases/sharing-kv-cache.html">vLLM Production Stack</a> documents KV cache sharing across vLLM instances using LMCache with various backends (Redis, Mooncake, etc.), providing the foundational architecture patterns.</p> <h3 id="key-insight-from-related-work">Key Insight from Related Work</h3> <p>A common finding across all implementations: <strong>local GPU APC is always checked first</strong> in vLLM V1. The cache hierarchy is strict — GPU HBM → CPU DRAM → External (Mooncake). This means cross-instance sharing only activates when local APC cannot serve the request, making it a complement to (not replacement for) local caching.</p> <h2 id="6-benchmark-measuring-the-impact">6. Benchmark: Measuring the Impact</h2> <h3 id="test-design">Test Design</h3> <p>We designed a controlled benchmark that isolates each caching layer’s contribution. Using long system prompts (~3,000 words / ~4,000+ tokens), we sent requests directly to specific prefill instances, bypassing the router:</p> <table> <thead> <tr> <th>Scenario</th> <th>Description</th> <th>What’s Tested</th> </tr> </thead> <tbody> <tr> <td>Cold</td> <td>First request, no cache anywhere</td> <td>Baseline prefill cost</td> </tr> <tr> <td>Same-instance (APC)</td> <td>Repeat to same instance</td> <td>Local GPU cache</td> </tr> <tr> <td>Cross + Mooncake</td> <td>Same prompt to different instance</td> <td>Distributed cache</td> </tr> <tr> <td>Cross - No cache</td> <td>Unique prompt to different instance</td> <td>Full recompute (control)</td> </tr> </tbody> </table> <h3 id="results">Results</h3> <table> <thead> <tr> <th>Scenario</th> <th>Avg TTFT</th> <th>Speedup vs Cold</th> </tr> </thead> <tbody> <tr> <td><strong>Cold (no cache)</strong></td> <td>0.808s</td> <td>—</td> </tr> <tr> <td><strong>Same-instance (local APC)</strong></td> <td>0.330s</td> <td><strong>2.45x</strong></td> </tr> <tr> <td><strong>Cross-instance + Mooncake (same node)</strong></td> <td>0.605s</td> <td><strong>1.34x</strong></td> </tr> <tr> <td><strong>Cross-instance + Mooncake (different node)</strong></td> <td>0.599s</td> <td><strong>1.35x</strong></td> </tr> <tr> <td><strong>Cross-instance - no cache</strong></td> <td>0.789s</td> <td>1.02x</td> </tr> </tbody> </table> <h3 id="key-takeaways">Key Takeaways</h3> <p><strong>Mooncake delivers 24% TTFT reduction</strong> in cross-instance scenarios (0.789s → 0.605s). When a prompt lands on an instance without local cache, Mooncake fetches pre-computed KV tensors instead of recomputing, saving ~0.19s per request.</p> <p><strong>Same-node vs different-node: negligible difference</strong> (0.605s vs 0.599s). Even over TCP, cross-node Mooncake transfer is fast enough. This suggests the bottleneck is not network transfer but rather the overhead of chunk lookup and GPU memory operations. Mooncake also supports RDMA by setting <code class="language-plaintext highlighter-rouge">protocol: "rdma"</code> in the LMCache config (requires RDMA-capable NICs and a compatible Mooncake build), though given the bottleneck is not network-bound, the practical benefit is likely limited except at very high request rates or large chunk sizes.</p> <p><strong>Local APC is still significantly faster</strong> (0.330s vs 0.605s). Mooncake adds ~84% overhead compared to local APC, making cache-aware routing critical for maximizing local hits. Mooncake serves as a fallback for requests that inevitably miss local cache.</p> <p><strong>Mooncake metrics confirmed actual cross-instance transfer</strong>:</p> <ul> <li>Batch Put: +12 requests, +110 items (KV chunks stored to pool)</li> <li>Batch Get: +12 requests, +156 items (KV chunks fetched cross-instance)</li> <li>Memory pool grew by 660 MB during the test</li> </ul> <h2 id="7-conclusion">7. Conclusion</h2> <p>Cross-instance KV cache sharing with Mooncake + LMCache fills an important gap in multi-instance LLM serving. While local APC handles the common case efficiently, the 24% TTFT improvement in cross-instance scenarios translates to meaningful latency savings at scale — especially as the number of prefill instances grows and per-instance cache hit rates naturally decline.</p> <p>The optimal setup combines three layers:</p> <ol> <li><strong>Local APC</strong> for same-instance cache hits (fastest)</li> <li><strong>Cache-aware routing</strong> (e.g., prefix-cache-scorer) to maximize local hits</li> <li><strong>Distributed KV cache</strong> (Mooncake) as a fallback for cross-instance misses</li> </ol> <p>For production deployments, we recommend starting with cache-aware routing to maximize local APC utilization, then adding Mooncake for the remaining cross-instance misses. The benefit scales with the diversity of prompts and the number of instances — workloads with many shared long prefixes (e.g., RAG with common document contexts) will see the greatest impact.</p>]]></content><author><name></name></author><category term="infrastructure"/><category term="gpu"/><category term="vllm"/><category term="llm"/><category term="inference"/><category term="rdma"/><category term="networking"/><summary type="html"><![CDATA[How cross-instance KV cache sharing with Mooncake + LMCache reduces TTFT by 24% in multi-instance disaggregated LLM serving]]></summary></entry><entry xml:lang="en"><title type="html">NIXL for KV Cache in Disaggregated Serving</title><link href="https://nsd9696.github.io/blog/2026/nixl-kv-cache-disaggregated-serving/" rel="alternate" type="text/html" title="NIXL for KV Cache in Disaggregated Serving"/><published>2026-03-04T00:00:00+00:00</published><updated>2026-03-04T00:00:00+00:00</updated><id>https://nsd9696.github.io/blog/2026/nixl-kv-cache-disaggregated-serving</id><content type="html" xml:base="https://nsd9696.github.io/blog/2026/nixl-kv-cache-disaggregated-serving/"><![CDATA[<h2 id="1-kv-cache-in-disaggregated-serving">1. KV Cache in Disaggregated Serving</h2> <p>In Prefill/Decode (P/D) disaggregated serving, the speed of KV Cache transfer from Prefill to Decode is critical. The per-token KV Cache size is calculated as:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Per-token KV Cache = num_layers × 2 (K, V) × num_kv_heads × head_dim × dtype_size
</code></pre></div></div> <p>For <strong>Llama-3.1-70B</strong> with BF16:</p> <table> <thead> <tr> <th>Parameter</th> <th>Value</th> <th>Description</th> </tr> </thead> <tbody> <tr> <td>Layers</td> <td>80</td> <td>Transformer blocks</td> </tr> <tr> <td>× 2</td> <td>K and V</td> <td>Each layer stores both Key and Value vectors</td> </tr> <tr> <td>KV Heads</td> <td>8</td> <td>GQA (Grouped Query Attention)</td> </tr> <tr> <td>Head Dim</td> <td>128</td> <td>Vector size per head</td> </tr> <tr> <td>dtype size</td> <td>2 bytes</td> <td>BF16 precision</td> </tr> </tbody> </table> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Per token = 80 × 2 × 8 × 128 × 2 = 327,680 bytes ≈ 320 KB
</code></pre></div></div> <p>That’s <strong>320 KB per token</strong>, multiplied by the prompt length:</p> <table> <thead> <tr> <th style="text-align: center">Prompt Length</th> <th style="text-align: center">KV Cache Size</th> <th style="text-align: center">IB HDR × 1 (200 Gbps, ~25 GB/s)</th> </tr> </thead> <tbody> <tr> <td style="text-align: center">1K tokens</td> <td style="text-align: center">0.32 GB</td> <td style="text-align: center">~13 ms</td> </tr> <tr> <td style="text-align: center">4K tokens</td> <td style="text-align: center">1.28 GB</td> <td style="text-align: center">~51 ms</td> </tr> <tr> <td style="text-align: center">8K tokens</td> <td style="text-align: center">2.56 GB</td> <td style="text-align: center">~102 ms</td> </tr> <tr> <td style="text-align: center">32K tokens</td> <td style="text-align: center">10.24 GB</td> <td style="text-align: center">~410 ms</td> </tr> <tr> <td style="text-align: center">128K tokens</td> <td style="text-align: center">40.96 GB</td> <td style="text-align: center">~1,638 ms</td> </tr> </tbody> </table> <p>These numbers make it clear: KV Cache transfer speed directly impacts P/D disaggregated serving latency. Several factors make this transfer challenging:</p> <ul> <li><strong>GPU-to-GPU memory</strong>: Direct transfer without CPU involvement (GPUDirect RDMA)</li> <li><strong>Non-contiguous memory</strong>: PagedAttention scatters KV Cache across non-contiguous blocks</li> <li><strong>Asynchronous execution</strong>: GPUs must continue processing other requests during transfer</li> <li><strong>Heterogeneous paths</strong>: NVLink, InfiniBand, RoCE, EFA, TCP — the optimal path varies by environment</li> <li><strong>Memory registration</strong>: RDMA requires registering memory with the NIC before transfer, which is expensive</li> </ul> <p>This is exactly what <strong>NIXL</strong> (NVIDIA Inference Xfer Library) is designed to solve — a library for accelerating data transfer in disaggregated serving. It is open-sourced at the <code class="language-plaintext highlighter-rouge">ai-dynamo/nixl</code> repository. (Note: NCCL is for collective operations like AllReduce and AllGather; NIXL is specifically for point-to-point data movement in serving.)</p> <hr/> <h2 id="2-nixl-architecture-overview">2. NIXL Architecture Overview</h2> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/blog_nixl_1-480.webp 480w,/assets/img/blog_nixl_1-800.webp 800w,/assets/img/blog_nixl_1-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/blog_nixl_1.png" class="img-fluid rounded z-depth-1 d-block mx-auto" width="80%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> NIXL Architecture — from inference frameworks down to hardware transport layers. </div> <p>NIXL’s <strong>Transfer Agent</strong> abstracts three core entities:</p> <h3 id="21-memory-section">2.1 Memory Section</h3> <p>Memory Section provides a unified abstraction over diverse memory and storage types. HBM (GPU), DRAM (CPU), NVMe SSD, file systems, and object storage are all handled through the same interface.</p> <h3 id="22-transfer-backend-interface">2.2 Transfer Backend Interface</h3> <p>The Transfer Backend Interface abstracts different transport backends from the Agent and selects the most optimal one for each transfer. For example:</p> <ul> <li>Source is DRAM, destination is VRAM → select <strong>UCX</strong></li> <li>VRAM to parallel file system → select <strong>GPUDirect Storage</strong></li> </ul> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/blog_nixl_2-480.webp 480w,/assets/img/blog_nixl_2-800.webp 800w,/assets/img/blog_nixl_2-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/blog_nixl_2.png" class="img-fluid rounded z-depth-1 d-block mx-auto" width="80%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> NIXL Backend Selection Flow — from memory type identification to final backend selection. </div> <p>When a user does not explicitly specify a backend, NIXL’s selection engine follows a 4-step process:</p> <ol> <li><strong>Identify memory types</strong> from local and remote descriptors (e.g., VRAM)</li> <li><strong>Find common backend engines</strong> — intersection of local and remote agent’s initialized backends</li> <li><strong>Verify memory registration</strong> — check if src/dst addresses fall within each candidate’s registered ranges</li> <li><strong>Final selection</strong> — usually only one backend qualifies; if multiple, use preference list</li> </ol> <h3 id="23-metadata-handler">2.3 Metadata Handler</h3> <p>The Metadata Handler encapsulates the information needed for data transfer between NIXL Agents running on different workers. It exchanges this information via side channels (HTTP, ZeroMQ, TCP) or centralized metadata services (etcd, Redis).</p> <hr/> <h2 id="3-nixl-internals">3. NIXL Internals</h2> <h3 id="31-transfer-agent">3.1 Transfer Agent</h3> <p>A Transfer Agent is the core runtime entity of NIXL. Each Agent is identified by a unique name (e.g., <code class="language-plaintext highlighter-rouge">"prefill_worker_0"</code>). A single Agent can manage multiple GPUs, and in vLLM’s case, one Agent is created per TP (Tensor Parallelism) worker.</p> <p>An Agent manages:</p> <ul> <li><strong>Local memory sections</strong>: Registered VRAM/DRAM/SSD address ranges and per-backend registration state</li> <li><strong>Backend engine instances</strong>: Initialized plugins (UCX, GDS, libfabric, etc.) with their connection info</li> <li><strong>Remote agent metadata cache</strong>: Connection info and memory layout received via <code class="language-plaintext highlighter-rouge">load_remote_md()</code> — required before sending transfers to a remote Agent</li> <li><strong>In-flight transfer requests</strong>: Status tracking (IN_PROGRESS, DONE, ERROR) of active handles</li> </ul> <h3 id="32-memory-section-and-descriptors">3.2 Memory Section and Descriptors</h3> <p>A <strong>Memory Section</strong> is a collection of memory registered with a NIXL Agent. Each segment is a <code class="language-plaintext highlighter-rouge">(address, length, device_id)</code> tuple.</p> <table> <thead> <tr> <th>Memory Type</th> <th>address</th> <th>device_id</th> <th>Use Case</th> </tr> </thead> <tbody> <tr> <td>VRAM</td> <td>GPU virtual addr</td> <td>GPU index</td> <td>KV Cache blocks</td> </tr> <tr> <td>DRAM</td> <td>CPU virtual addr</td> <td>NUMA node</td> <td>CPU offload buffer</td> </tr> <tr> <td>NVMe</td> <td>file offset</td> <td>disk index</td> <td>SSD checkpoint</td> </tr> <tr> <td>Object Store</td> <td>bucket/key</td> <td>S3 endpoint</td> <td>Object storage</td> </tr> </tbody> </table> <p>A <strong>Descriptor List</strong> is the basic unit of transfer requests, coming in two varieties:</p> <ul> <li><strong>Registration descriptor</strong>: <code class="language-plaintext highlighter-rouge">(addr, len, devID, optional_str)</code></li> <li><strong>Transfer descriptor</strong>: <code class="language-plaintext highlighter-rouge">(addr, len, devID, backend_metadata)</code></li> </ul> <p>Importantly, descriptors within a single Descriptor List <strong>can span different GPUs</strong>. A single transfer request can include 3 blocks from GPU 0 + 2 blocks from GPU 1, and NIXL processes them in parallel.</p> <h3 id="33-backend-plugin-south-bound-api">3.3 Backend Plugin (South Bound API)</h3> <p>NIXL’s backend plugins implement the South Bound API (SB API), and each plugin is loaded on demand.</p> <p>Currently supported backends (NIXL 0.8.0):</p> <table> <thead> <tr> <th>Backend</th> <th>Transport Path</th> </tr> </thead> <tbody> <tr> <td>UCX</td> <td>RDMA, NVLink, TCP</td> </tr> <tr> <td>GPUNetIO (DOCA)</td> <td>GDAKI, CPU bypass</td> </tr> <tr> <td>Libfabric</td> <td>EFA, CXI, SRD</td> </tr> <tr> <td>GDS / GDS_MT</td> <td>GPUDirect Storage</td> </tr> <tr> <td>POSIX</td> <td>AIO, io_uring</td> </tr> <tr> <td>OBJ</td> <td>S3 API</td> </tr> <tr> <td>Mooncake</td> <td>Third-party transfer engine</td> </tr> </tbody> </table> <p>Each plugin implements these core SB API functions:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>initBackend()      — Initialize backend, establish connections
registerMem()      — Register memory regions (e.g., RDMA MR registration)
deregisterMem()    — Deregister memory
prepXferReq()      — Prepare transfer request (resource allocation)
postXferReq()      — Start transfer (async)
checkXferReq()     — Check transfer status
releaseXferReq()   — Release transfer handle
</code></pre></div></div> <h3 id="34-north-bound-api-user-interface">3.4 North Bound API (User Interface)</h3> <p>The North Bound API is what inference frameworks (vLLM, etc.) use. It follows a 5-phase lifecycle:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># ─── Phase 1: Backend Initialization ───
</span><span class="n">agent</span> <span class="o">=</span> <span class="nc">NixlAgent</span><span class="p">(</span><span class="sh">"</span><span class="s">prefill_worker_0</span><span class="sh">"</span><span class="p">,</span> <span class="n">backends</span><span class="o">=</span><span class="p">[</span><span class="sh">"</span><span class="s">UCX</span><span class="sh">"</span><span class="p">])</span>

<span class="c1"># ─── Phase 2: Memory Registration ───
# Internally performs RDMA Memory Region registration (pin + physical address mapping).
# Called once at server startup; no need to re-register per transfer.
</span><span class="n">reg_desc</span> <span class="o">=</span> <span class="n">agent</span><span class="p">.</span><span class="nf">register_memory</span><span class="p">(</span><span class="n">memory_section</span><span class="p">)</span>

<span class="c1"># ─── Phase 3: Metadata Exchange ───
# Serialize local Agent's connection info + registered memory info.
# Send this byte string to the remote Agent via side channel (HTTP, etcd, etc.).
</span><span class="n">local_md</span> <span class="o">=</span> <span class="n">agent</span><span class="p">.</span><span class="nf">get_local_md</span><span class="p">()</span>        <span class="c1"># Serialized metadata (bytes)
</span><span class="n">agent</span><span class="p">.</span><span class="nf">load_remote_md</span><span class="p">(</span><span class="n">remote_agent_md</span><span class="p">)</span>  <span class="c1"># Load remote agent info
# Only after load can you create transfer requests to that remote Agent.
</span>
<span class="c1"># ─── Phase 4: Create and Execute Transfer ───
# create: backend selection + resource allocation → returns handle (transfer not started yet)
</span><span class="n">handle</span> <span class="o">=</span> <span class="n">agent</span><span class="p">.</span><span class="nf">create_xfer_req</span><span class="p">(</span><span class="n">local_descs</span><span class="p">,</span> <span class="n">remote_descs</span><span class="p">,</span> <span class="sh">"</span><span class="s">decode_worker_0</span><span class="sh">"</span><span class="p">,</span> <span class="n">WRITE</span><span class="p">)</span>
<span class="c1"># post: actually starts the async transfer
</span><span class="n">agent</span><span class="p">.</span><span class="nf">post_xfer_req</span><span class="p">(</span><span class="n">handle</span><span class="p">)</span>

<span class="c1"># ─── Phase 5: Completion Check (non-blocking) ───
# Poll until transfer completes. GPU is free to do other work in the meantime.
</span><span class="k">while</span> <span class="n">agent</span><span class="p">.</span><span class="nf">get_xfer_status</span><span class="p">(</span><span class="n">handle</span><span class="p">)</span> <span class="o">==</span> <span class="n">IN_PROGRESS</span><span class="p">:</span>
    <span class="k">pass</span>  <span class="c1"># Or perform prefill for other requests
# Release handle after completion
</span><span class="n">agent</span><span class="p">.</span><span class="nf">release_xfer_req</span><span class="p">(</span><span class="n">handle</span><span class="p">)</span>
</code></pre></div></div> <p>The key design is that <strong>create and post are separated</strong>. <code class="language-plaintext highlighter-rouge">create</code> handles backend selection and resource allocation upfront, while <code class="language-plaintext highlighter-rouge">post</code> triggers the actual data movement. This separation enables pre-allocation of resources and immediate, low-latency transfer initiation.</p> <p>Combining these three entities, the Transfer Agent accepts <strong>buffer list primitives</strong> (non-contiguous GPU memory address lists from PagedAttention) and returns an <strong>async handle</strong>. All transfers are non-blocking — <code class="language-plaintext highlighter-rouge">post_xfer_req()</code> returns immediately, and <code class="language-plaintext highlighter-rouge">get_xfer_status()</code> checks completion — preventing the GPU from sitting idle during transfer.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/blog_nixl_3-480.webp 480w,/assets/img/blog_nixl_3-800.webp 800w,/assets/img/blog_nixl_3-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/blog_nixl_3.png" class="img-fluid rounded z-depth-1 d-block mx-auto" width="80%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> NIXL Transfer Request Lifecycle — from composing buffer lists to release. </div> <p><strong>READ vs WRITE</strong>: WRITE means the Prefill Worker pushes its KV Cache directly into the Decode Worker’s memory (push model). READ means the Decode Worker pulls from the Prefill Worker’s memory (pull model). Both are RDMA one-sided operations requiring no CPU involvement on the remote side. vLLM’s NixlConnector uses the WRITE mode.</p> <hr/> <h2 id="4-vllm-nixlconnector--kv-cache-transfer-in-practice">4. vLLM NixlConnector — KV Cache Transfer in Practice</h2> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Prefill instance</span>
<span class="nv">CUDA_VISIBLE_DEVICES</span><span class="o">=</span>0 <span class="se">\</span>
<span class="nv">UCX_NET_DEVICES</span><span class="o">=</span>all <span class="se">\</span>
<span class="nv">VLLM_NIXL_SIDE_CHANNEL_PORT</span><span class="o">=</span>5601 <span class="se">\</span>
vllm serve meta-llama/Llama-3.1-70B-Instruct <span class="se">\</span>
  <span class="nt">--port</span> 8100 <span class="se">\</span>
  <span class="nt">--kv-transfer-config</span> <span class="s1">'{
    "kv_connector": "NixlConnector",
    "kv_role": "kv_both"
  }'</span>

<span class="c"># Decode instance</span>
<span class="nv">CUDA_VISIBLE_DEVICES</span><span class="o">=</span>1 <span class="se">\</span>
<span class="nv">UCX_NET_DEVICES</span><span class="o">=</span>all <span class="se">\</span>
<span class="nv">VLLM_NIXL_SIDE_CHANNEL_PORT</span><span class="o">=</span>5602 <span class="se">\</span>
vllm serve meta-llama/Llama-3.1-70B-Instruct <span class="se">\</span>
  <span class="nt">--port</span> 8200 <span class="se">\</span>
  <span class="nt">--kv-transfer-config</span> <span class="s1">'{
    "kv_connector": "NixlConnector",
    "kv_role": "kv_both"
  }'</span>

<span class="c"># Proxy server (request routing)</span>
python toy_proxy_server.py <span class="se">\</span>
  <span class="nt">--port</span> 8192 <span class="se">\</span>
  <span class="nt">--prefiller-hosts</span> localhost <span class="nt">--prefiller-ports</span> 8100 <span class="se">\</span>
  <span class="nt">--decoder-hosts</span> localhost <span class="nt">--decoder-ports</span> 8200
</code></pre></div></div> <h3 id="41-vllms-kv-connector-architecture">4.1 vLLM’s KV Connector Architecture</h3> <p>vLLM’s Disaggregated Serving supports various transfer backends through the <code class="language-plaintext highlighter-rouge">KVConnector</code> abstraction:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>vLLM KV Connector Plugins:
├── NixlConnector      — NIXL-based, fully async (recommended default)
├── LMCacheConnectorV1 — LMCache library (can use NIXL internally)
├── P2pNcclConnector   — NCCL P2P-based
└── MooncakeConnector  — Mooncake Transfer Engine
</code></pre></div></div> <p>The NixlConnector is implemented in <code class="language-plaintext highlighter-rouge">vllm/distributed/kv_transfer/kv_connector/v1/nixl_connector.py</code> and operates at two layers:</p> <ol> <li><strong>Scheduler Connector</strong> (EngineCore process): <ul> <li>Decides which requests’ KV Cache to transfer</li> <li>Transfer scheduling (block mapping, timing)</li> <li>Transfer completion tracking</li> </ul> </li> <li><strong>Worker Connector</strong> (GPU Worker process): <ul> <li>Manages the actual NIXL Agent</li> <li>Memory registration and metadata exchange</li> <li>Async transfer execution and status checking</li> </ul> </li> </ol> <h3 id="42-key-configuration-details">4.2 Key Configuration Details</h3> <ul> <li><strong><code class="language-plaintext highlighter-rouge">kv_role: "kv_both"</code></strong>: A single instance can perform both Prefill and Decode. This provides flexibility — the Proxy server can dynamically decide which instance handles Prefill and which handles Decode.</li> <li><strong><code class="language-plaintext highlighter-rouge">VLLM_NIXL_SIDE_CHANNEL_PORT</code></strong>: Each instance requires a unique port. In TP/DP deployments, each rank needs its own port.</li> </ul> <hr/> <h2 id="5-debugging-ucx-memory-leak-in-disaggregated-serving">5. Debugging: UCX Memory Leak in Disaggregated Serving</h2> <h3 id="51-symptom">5.1 Symptom</h3> <p>A memory leak was observed that could not be reproduced with a single vLLM instance or a simple UCX test tool. It only occurred when <strong>all</strong> of the following conditions were met:</p> <ul> <li>vLLM + <strong>NIXL</strong> + <strong>UCX</strong> (InfiniBand) in P/D disaggregated setup</li> <li>System memory (RSS) grew linearly at <strong>~400 MB/min</strong> on the <strong>Decode instance only</strong></li> <li>Prefill side was normal → the problem was exclusive to the <strong>receiving side</strong> of KV Cache transfers</li> <li>Eventually led to OOM (Out of Memory) after several hours</li> </ul> <p>The fact that it only occurred on the Decode side was the key clue. In P/D disaggregation, the Decode instance receives KV Cache from Prefill via NIXL (→ UCX → InfiniBand RDMA), making the KV Cache transfer path the prime suspect.</p> <h3 id="52-root-cause-ucxs-mmap-hooking-mechanism">5.2 Root Cause: UCX’s mmap Hooking Mechanism</h3> <h4 id="why-ucx-hooks-mmap">Why UCX hooks mmap</h4> <p>UCX maintains a <strong>Registration Cache (RCache)</strong> for InfiniBand RDMA transfer performance. RDMA requires registering memory with the NIC before transfer, and this registration is expensive. RCache avoids re-registering the same memory regions by caching previously registered mappings.</p> <p>The problem is: if the application calls <code class="language-plaintext highlighter-rouge">munmap</code> on a region that’s still in the RCache, the physical memory gets reassigned. If UCX later uses the stale cached registration, the NIC would read or overwrite memory that now belongs to something else — a correctness disaster.</p> <p>So UCX needs to know when the application frees memory. But UCX is just a library — it has no built-in way to know when <code class="language-plaintext highlighter-rouge">munmap</code> is called. UCX’s memory management module (UCM) solves this by <strong>dynamically patching the process’s GOT (Global Offset Table)</strong> at runtime, intercepting all <code class="language-plaintext highlighter-rouge">mmap</code>/<code class="language-plaintext highlighter-rouge">munmap</code> calls process-wide.</p> <p>The critical issue: this hooking intercepts <strong>all</strong> mmap/munmap calls in the process, not just UCX-related ones. Python interpreter, PyTorch, and every other library’s mmap calls all go through UCX.</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Normal mmap call path:
  Application → glibc mmap() → kernel syscall

After UCX hooking:
  Application → UCX-hooked mmap() → UCX RCache management → kernel syscall
</code></pre></div></div> <h4 id="stack-trace-evidence">Stack trace evidence</h4> <p><strong>Stack Trace 1</strong>: Python’s memory allocator calling mmap, routed through UCX’s hook:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>#0  syscall()
#1  ucm_orig_mmap_syscall()    ← UCX's mmap hook intercept
#4  ucm_mmap()                 ← UCX-intercepted mmap
#5  _PyMem_ArenaAlloc()        ← Python memory manager
</code></pre></div></div> <p><strong>Stack Trace 2</strong>: Python calling munmap (memory free) but UCX internally triggering mmap (memory allocation):</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>#0  syscall()
#1  ucm_orig_mmap_syscall()    ← mmap happening INSIDE munmap!
#3  ucm_munmap()               ← Original munmap call
</code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">munmap</code> is supposed to free memory, but during that process, UCX was allocating new memory. This was the source of the leak.</p> <h3 id="53-why-mmap-happens-inside-munmap">5.3 Why mmap Happens Inside munmap</h3> <p>When <code class="language-plaintext highlighter-rouge">munmap</code> is called, UCX doesn’t immediately delete the corresponding RCache entry — it needs to communicate with the NIC, which is expensive. Instead, it puts the entry into an <strong>invalidation queue</strong> to be cleaned up later during <code class="language-plaintext highlighter-rouge">ucp_worker_progress()</code> calls.</p> <p>This queue itself is managed by UCX’s memory pool (<code class="language-plaintext highlighter-rouge">ucs_mpool</code>). When entries accumulate and the pool fills up, it calls <code class="language-plaintext highlighter-rouge">mmap</code> to grow — allocating new memory.</p> <p>Under normal operation, <code class="language-plaintext highlighter-rouge">ucp_worker_progress()</code> should drain the invalidation queue. NIXL and vLLM did call this function, but in this specific edge case, the cleanup was not triggered. Combined with <code class="language-plaintext highlighter-rouge">UCX_RCACHE_MAX_UNRELEASED</code> defaulting to <strong>infinity</strong>, the queue grew without bound.</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>munmap called by Python/PyTorch
  │
  ▼
UCX intercepts → adds RCache entry to invalidation queue
  │
  ▼
Queue grows → ucs_mpool capacity exceeded
  │
  ▼
ucs_mpool_grow() → mmap call (new memory allocation)
  │
  ▼
This new memory also gets hooked by UCX → registered in RCache
  │
  ▼
Queue cleanup never triggers → entries keep accumulating
  │
  ▼
RSS grows linearly (~400 MB/min)
</code></pre></div></div> <h3 id="54-the-fix">5.4 The Fix</h3> <p><strong>Fix 1: Disable mmap hooking</strong></p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">export </span><span class="nv">UCX_MEM_MMAP_HOOK_MODE</span><span class="o">=</span>none
</code></pre></div></div> <p>This completely disables UCX’s mmap/munmap hooking. The memory leak was <strong>immediately resolved</strong>, with <strong>no performance impact</strong>.</p> <p>Why no performance degradation? vLLM’s KV Cache is a <strong>single large contiguous memory block pool</strong> allocated once at server startup. NIXL registers this memory once, and all subsequent transfers use the already-registered region. There’s no dynamic memory that needs RCache tracking during steady-state operation.</p> <p><strong>Fix 2: Limit invalidation queue size</strong></p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">export </span><span class="nv">UCX_RCACHE_MAX_UNRELEASED</span><span class="o">=</span>1024  <span class="c"># default: infinity</span>
</code></pre></div></div> <p>This caps the invalidation queue, forcing UCX to start cleanup when the threshold is reached. This preserves hooking while preventing unbounded growth.</p> <p><strong>Upstream fixes</strong>: vLLM now includes <code class="language-plaintext highlighter-rouge">UCX_MEM_MMAP_HOOK_MODE=none</code> as a default setting (<a href="https://github.com/vllm-project/vllm/pull/32181">PR #32181</a>), and NIXL/UCX decided to change <code class="language-plaintext highlighter-rouge">UCX_RCACHE_MAX_UNRELEASED</code> from infinity to a finite default value (<a href="https://github.com/ai-dynamo/nixl/pull/1210">NIXL PR #1210</a>).</p>]]></content><author><name></name></author><category term="infrastructure"/><category term="gpu"/><category term="networking"/><category term="nixl"/><category term="vllm"/><category term="llm"/><category term="inference"/><category term="rdma"/><summary type="html"><![CDATA[How NIXL accelerates KV Cache transfer in Prefill/Decode disaggregated LLM serving, its architecture, vLLM integration, and a real-world memory leak debugging story]]></summary></entry><entry xml:lang="en"><title type="html">CUDA Graph in vLLM: Eliminating CPU Overhead in LLM Inference</title><link href="https://nsd9696.github.io/blog/2026/cuda-graph-in-vllm/" rel="alternate" type="text/html" title="CUDA Graph in vLLM: Eliminating CPU Overhead in LLM Inference"/><published>2026-02-28T00:00:00+00:00</published><updated>2026-02-28T00:00:00+00:00</updated><id>https://nsd9696.github.io/blog/2026/cuda-graph-in-vllm</id><content type="html" xml:base="https://nsd9696.github.io/blog/2026/cuda-graph-in-vllm/"><![CDATA[<h2 id="1-why-cuda-graph--cpu-launch-overhead">1. Why CUDA Graph? — CPU Launch Overhead</h2> <p>Launching a single GPU kernel requires significant preparation on the CPU side. The host driver must load the kernel function, perform various validations, configure grid/block dimensions, allocate memory accordingly, and submit the kernel to a stream. While this overhead is only on the order of milliseconds per kernel, in patterns like LLM decode — where hundreds of short kernels execute in rapid succession — the cumulative CPU-side overhead causes substantial GPU idle time.</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Traditional Execution (Sequential Kernel Launch):

CPU:  [Launch A][Launch B][Launch C][Launch D][Launch E] ...
GPU:  ──[A]──gap──[B]──gap──[C]──gap──[D]──gap──[E]──
            ↑       ↑       ↑       ↑       ↑
         GPU idle between each kernel launch
</code></pre></div></div> <p>CUDA Graph solves this by <strong>capturing</strong> the entire sequence of kernel launches and then <strong>replaying</strong> it in one shot via <code class="language-plaintext highlighter-rouge">cudaGraphLaunch()</code>. Instead of the CPU issuing individual launch commands for each kernel, the driver submits the entire pre-recorded execution plan at once, virtually eliminating the per-kernel launch overhead.</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>CUDA Graph Execution (Single Graph Launch):

CPU:  [Graph Launch]
GPU:  ──[A][B][C][D][E]──
         ↑
      All kernels dispatched at once, no gaps
</code></pre></div></div> <h3 id="node-types-in-a-cuda-graph">Node Types in a CUDA Graph</h3> <p>A CUDA Graph is a DAG (Directed Acyclic Graph) where each node represents an operation. CUDA supports several node types:</p> <table> <thead> <tr> <th>Type</th> <th>API</th> <th>Parameters</th> </tr> </thead> <tbody> <tr> <td><strong>Kernel</strong></td> <td><code class="language-plaintext highlighter-rouge">cudaGraphAddKernelNode</code></td> <td>Function pointer, grid/block dim, shared memory, kernel args</td> </tr> <tr> <td><strong>Memcpy</strong></td> <td><code class="language-plaintext highlighter-rouge">cudaGraphAddMemcpyNode</code></td> <td>src/dst pointer, size, direction (H2D/D2H/D2D)</td> </tr> <tr> <td><strong>Memset</strong></td> <td><code class="language-plaintext highlighter-rouge">cudaGraphAddMemsetNode</code></td> <td>dst pointer, value, size</td> </tr> <tr> <td><strong>Host</strong></td> <td><code class="language-plaintext highlighter-rouge">cudaGraphAddHostNode</code></td> <td>CPU callback function and user data</td> </tr> <tr> <td><strong>MemAlloc</strong></td> <td><code class="language-plaintext highlighter-rouge">cudaGraphAddMemAllocNode</code></td> <td>Allocation size, attributes (CUDA 11.4+)</td> </tr> <tr> <td><strong>MemFree</strong></td> <td><code class="language-plaintext highlighter-rouge">cudaGraphAddMemFreeNode</code></td> <td>Target pointer</td> </tr> <tr> <td><strong>Child Graph</strong></td> <td><code class="language-plaintext highlighter-rouge">cudaGraphAddChildGraphNode</code></td> <td>Sub-graph</td> </tr> </tbody> </table> <hr/> <h2 id="2-cuda-graph-dag-in-llm-decode">2. CUDA Graph DAG in LLM Decode</h2> <p>To visualize what a CUDA Graph DAG looks like for the LLM decode stage, consider the following pipeline. Each box represents a graph node with its operation type, kernel function, launch configuration, and arguments:</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/blog_cuda_graph_1-480.webp 480w,/assets/img/blog_cuda_graph_1-800.webp 800w,/assets/img/blog_cuda_graph_1-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/blog_cuda_graph_1.png" class="img-fluid rounded z-depth-1 d-block mx-auto" width="80%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> CUDA Graph DAG for a single Transformer decoder layer during inference. Root nodes (Memset and H2D Memcpy) have no dependencies and can execute concurrently. RoPE for Q and K also fork in parallel before joining at the Flash Attention node. </div> <p>Key observations:</p> <ul> <li><strong>Root nodes execute concurrently</strong>: The Memset (zero output buffer) and Memcpy H2D (token embeddings) nodes have no dependencies and can run in parallel.</li> <li><strong>Fork/Join parallelism</strong>: RoPE for Q and K are independent and execute on parallel branches before synchronizing at the Flash Attention node.</li> <li><strong>The entire pipeline is a single graph launch</strong>: From embedding transfer to logits D2H copy, every operation is captured as one graph and replayed with a single <code class="language-plaintext highlighter-rouge">cudaGraphLaunch()</code> call.</li> </ul> <hr/> <h2 id="3-cuda-graph-lifecycle">3. CUDA Graph Lifecycle</h2> <p>The lifecycle of a CUDA Graph consists of three phases:</p> <ol> <li><strong>Definition</strong>: Create a graph template (<code class="language-plaintext highlighter-rouge">cudaGraph_t</code>). This is the “blueprint” phase where you define what operations to perform, their parameters, and their dependency relationships.</li> <li><strong>Instantiation</strong>: Convert the graph template into an executable graph (<code class="language-plaintext highlighter-rouge">cudaGraphExec_t</code>). During this step, the driver performs snapshot validation, resource allocation, and other preparations.</li> <li><strong>Execution</strong>: Submit the executable graph to a stream via <code class="language-plaintext highlighter-rouge">cudaGraphLaunch()</code>.</li> </ol> <h3 id="handling-dynamic-parameters">Handling Dynamic Parameters</h3> <p>In practice, CUDA Graphs must deal with changing conditions. During decode, the number of requests being processed (batch size) changes, which alters grid dimensions. Parameters like <code class="language-plaintext highlighter-rouge">learning_rate</code> (training) or <code class="language-plaintext highlighter-rouge">temperature</code> (inference) can also vary between iterations. Re-capturing and re-instantiating the graph every time would negate the performance benefits.</p> <p>CUDA provides <strong>graph update</strong> mechanisms for this. <code class="language-plaintext highlighter-rouge">cudaGraphExecUpdate()</code> allows modifying parameters of an already-instantiated executable graph (e.g., kernel arguments, grid dimensions) without going through the full capture-instantiate cycle again — as long as the graph topology remains the same.</p> <h3 id="graph-construction-methods">Graph Construction Methods</h3> <h4 id="explicit-api">Explicit API</h4> <p>Create an empty graph with <code class="language-plaintext highlighter-rouge">cudaGraphCreate()</code> and add nodes one by one with <code class="language-plaintext highlighter-rouge">cudaGraphAddKernelNode()</code>. This gives fine-grained control over graph structure but is impractical when hundreds of kernels are dynamically determined at runtime.</p> <div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">cudaKernelNodeParams</span> <span class="n">kParams</span> <span class="o">=</span> <span class="p">{</span><span class="mi">0</span><span class="p">};</span>
<span class="n">kParams</span><span class="p">.</span><span class="n">func</span> <span class="o">=</span> <span class="p">(</span><span class="kt">void</span><span class="o">*</span><span class="p">)</span><span class="n">myKernel</span><span class="p">;</span>       <span class="c1">// kernel function pointer</span>
<span class="n">kParams</span><span class="p">.</span><span class="n">gridDim</span> <span class="o">=</span> <span class="n">dim3</span><span class="p">(</span><span class="n">blocks</span><span class="p">,</span> <span class="mi">1</span><span class="p">,</span> <span class="mi">1</span><span class="p">);</span>
<span class="n">kParams</span><span class="p">.</span><span class="n">blockDim</span> <span class="o">=</span> <span class="n">dim3</span><span class="p">(</span><span class="mi">256</span><span class="p">,</span> <span class="mi">1</span><span class="p">,</span> <span class="mi">1</span><span class="p">);</span>
<span class="n">kParams</span><span class="p">.</span><span class="n">sharedMemBytes</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>
<span class="kt">void</span><span class="o">*</span> <span class="n">args</span><span class="p">[]</span> <span class="o">=</span> <span class="p">{</span><span class="o">&amp;</span><span class="n">d_out</span><span class="p">,</span> <span class="o">&amp;</span><span class="n">d_in</span><span class="p">,</span> <span class="o">&amp;</span><span class="n">size</span><span class="p">};</span>
<span class="n">kParams</span><span class="p">.</span><span class="n">kernelParams</span> <span class="o">=</span> <span class="n">args</span><span class="p">;</span>           <span class="c1">// pointer addresses copied by value</span>

<span class="n">cudaGraphNode_t</span> <span class="n">node</span><span class="p">;</span>
<span class="n">cudaGraphAddKernelNode</span><span class="p">(</span><span class="o">&amp;</span><span class="n">node</span><span class="p">,</span> <span class="n">graph</span><span class="p">,</span> <span class="nb">NULL</span><span class="p">,</span> <span class="mi">0</span><span class="p">,</span> <span class="o">&amp;</span><span class="n">kParams</span><span class="p">);</span>
</code></pre></div></div> <h4 id="stream-capture">Stream Capture</h4> <p>Stream Capture records all CUDA operations submitted to a stream during a capture region and automatically builds the graph. This is the method used in practice for deep learning frameworks:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># PyTorch stream capture
</span><span class="n">g</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="n">cuda</span><span class="p">.</span><span class="nc">CUDAGraph</span><span class="p">()</span>
<span class="k">with</span> <span class="n">torch</span><span class="p">.</span><span class="n">cuda</span><span class="p">.</span><span class="nf">graph</span><span class="p">(</span><span class="n">g</span><span class="p">):</span>
    <span class="n">output</span> <span class="o">=</span> <span class="nf">model</span><span class="p">(</span><span class="n">static_input</span><span class="p">)</span>   <span class="c1"># All CUDA ops inside are recorded
</span><span class="n">g</span><span class="p">.</span><span class="nf">replay</span><span class="p">()</span>                         <span class="c1"># Replay the captured graph
</span></code></pre></div></div> <h3 id="what-does-a-captured-graph-contain">What Does a Captured Graph Contain?</h3> <p>Each kernel node in a captured graph stores:</p> <ol> <li><strong>Kernel function pointer</strong> (<code class="language-plaintext highlighter-rouge">void* func</code>): Points to the memory address of the loaded kernel binary — identifies which GPU function to call.</li> <li><strong>gridDim</strong>: Number of thread blocks.</li> <li><strong>blockDim</strong>: Number of threads per block.</li> <li><strong>sharedMemBytes</strong>: Shared memory size.</li> <li><strong>Arguments</strong>: All kernel arguments, including both scalar values and pointers.</li> </ol> <hr/> <h2 id="4-memory-management-in-cuda-graph-private-pools">4. Memory Management in CUDA Graph: Private Pools</h2> <h3 id="the-problem-with-pytorchs-default-allocator">The Problem with PyTorch’s Default Allocator</h3> <p>PyTorch uses <code class="language-plaintext highlighter-rouge">CUDACachingAllocator</code> instead of raw <code class="language-plaintext highlighter-rouge">cudaMalloc()</code> calls, because the traditional allocation path (CPU -&gt; OS kernel -&gt; GPU driver -&gt; GPU hardware) is inherently slow. The caching allocator retains freed GPU memory in an internal pool rather than returning it to the OS, enabling fast reuse.</p> <p>However, this creates a problem when combined with CUDA Graphs:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>1. CUDA Graph Capture:
   with torch.cuda.graph(g):
       temp = model(input)      # temp allocated at 0x...C000
       output = process(temp)   # output allocated at 0x...D000
       del temp                 # 0x...C000 returned to pool

   -&gt; Graph records addresses: 0x...C000 (temp), 0x...D000 (output)

2. After capture, normal code runs:
   something = torch.randn(same_size, device='cuda')
   -&gt; Pool searches for free block -&gt; finds 0x...C000 -&gt; reuses it
   -&gt; something's address = 0x...C000

3. Graph replay:
   g.replay()
   -&gt; Graph writes temp data to 0x...C000
   -&gt; CORRUPTS 'something' tensor!
</code></pre></div></div> <p>The graph “remembers” specific memory addresses from capture time, but the general-purpose pool doesn’t know those addresses are reserved.</p> <h3 id="the-solution-private-pools">The Solution: Private Pools</h3> <p>To solve this, CUDA Graph capture uses <strong>Private Pools</strong> — dedicated memory pools that are exclusively reserved for graph operations:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>┌───────────────────────────────────────────┐
│              GPU Memory                    │
│                                            │
│  ┌──────────────────┐  ┌────────────────┐ │
│  │ Global Pool       │  │ Private Pool   │ │
│  │                    │  │ (Graph-only)   │ │
│  │ General tensor     │  │                │ │
│  │ allocation         │  │ External       │ │
│  │ Anyone can reuse   │  │ access blocked │ │
│  │                    │  │ Only graph can │ │
│  │                    │  │ use this pool  │ │
│  └──────────────────┘  └────────────────┘ │
└───────────────────────────────────────────┘
</code></pre></div></div> <p>With Private Pools:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>1. Capture:
   temp = model(input)      # Private Pool allocates 0x...C000
   output = process(temp)   # Private Pool allocates 0x...D000
   del temp                 # 0x...C000 returned to Private Pool

2. After capture, normal code:
   something = torch.randn(same_size, device='cuda')
   -&gt; Searches Global Pool only -&gt; Private Pool excluded
   -&gt; Allocates 0x...B000 from Global Pool
   -&gt; 0x...C000 is untouched

3. Graph replay:
   g.replay()
   -&gt; 0x...C000 is still graph-exclusive -&gt; safe to use
</code></pre></div></div> <hr/> <h2 id="5-cuda-graph-in-vllm">5. CUDA Graph in vLLM</h2> <h3 id="vllms-memory-architecture">vLLM’s Memory Architecture</h3> <p>vLLM uses PagedAttention for efficient KV Cache management. The KV Cache is managed as fixed-size blocks, and the <strong>base pointers</strong> of these blocks are allocated once during model initialization and remain fixed. What changes between iterations is only the <strong>block table</strong> — the mapping of which logical blocks point to which physical blocks. Since the base pointers don’t change, they are safe to hardcode in a captured CUDA Graph.</p> <h3 id="vllms-graph-dispatch-architecture">vLLM’s Graph Dispatch Architecture</h3> <p>vLLM caches graphs per batch size. On a cache miss, it captures a new graph; on a cache hit, it updates static buffers and replays.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/blog_cuda_graph_2-480.webp 480w,/assets/img/blog_cuda_graph_2-800.webp 800w,/assets/img/blog_cuda_graph_2-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/blog_cuda_graph_2.png" class="img-fluid rounded z-depth-1 d-block mx-auto" width="80%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> vLLM CUDA Graph execution stack. The CudagraphDispatcher analyzes each batch and selects the runtime mode. The CUDAGraphWrapper manages per-batch-size graph caching, capturing new graphs on cache miss and replaying on cache hit. </div> <h3 id="why-attention-is-problematic-for-cuda-graphs">Why Attention Is Problematic for CUDA Graphs</h3> <p>Attention operations in LLMs have several properties that conflict with CUDA Graph’s static nature:</p> <ol> <li><strong>Variable sequence lengths</strong>: In prefill, input lengths differ per request, causing the attention kernel’s grid dimensions to change.</li> <li><strong>Dynamic KV Cache indexing</strong>: PagedAttention’s block addresses and block tables change between iterations. Capturing the entire attention within a single graph requires padding inputs to remove variability — but this is only supported by certain attention backends.</li> </ol> <h3 id="flash-attention-2-vs-flash-attention-3">Flash-Attention-2 vs Flash-Attention-3</h3> <p><strong>Flash-Attention-2</strong>:</p> <ul> <li>Grid dimensions change based on the requested sequence lengths.</li> <li>Variable-length requests are packed using a <code class="language-plaintext highlighter-rouge">cu_seqlens</code> array, and the grid dimension varies with the array length.</li> <li>As a result, <strong>prefill with Flash-Attention-2 cannot be graph-captured</strong>.</li> </ul> <p><strong>Flash-Attention-3</strong>:</p> <ul> <li>Designed from the ground up with CUDA Graph compatibility in mind.</li> <li>Uses a <strong>fixed maximum grid dimension</strong> regardless of actual sequence lengths.</li> <li>When the actual workload is smaller than the maximum, the remaining threads simply pad/idle.</li> <li>This enables <strong>full graph capture even during prefill</strong>.</li> </ul> <h3 id="vllms-cuda-graph-modes">vLLM’s CUDA Graph Modes</h3> <p>vLLM provides five compilation modes to handle the tension between graph capture and attention flexibility:</p> <table> <thead> <tr> <th>Mode</th> <th>Behavior</th> <th>Best For</th> </tr> </thead> <tbody> <tr> <td><strong>NONE</strong></td> <td>Eager execution (no graphs)</td> <td>Debugging</td> </tr> <tr> <td><strong>PIECEWISE</strong></td> <td>Captures everything except attention</td> <td>Most attention backends</td> </tr> <tr> <td><strong>FULL</strong></td> <td>Captures entire forward pass as one graph</td> <td>Compatible backends (e.g., FlashAttention-3)</td> </tr> <tr> <td><strong>FULL_DECODE_ONLY</strong></td> <td>Full graph for decode only, no graph for prefill</td> <td>P/D disaggregated serving (saves memory)</td> </tr> <tr> <td><strong>FULL_AND_PIECEWISE</strong></td> <td>Full graph for decode, piecewise for prefill</td> <td><strong>vLLM v1 default</strong> — best overall performance</td> </tr> </tbody> </table> <p><strong>PIECEWISE</strong> mode splits the forward pass at attention boundaries. Everything before and after attention is graph-captured, while attention itself runs eagerly. This provides most of the CUDA Graph benefits while maintaining compatibility with backends that have dynamic grid dimensions.</p> <p><strong>FULL</strong> mode captures the entire forward pass including attention. This requires an attention backend that supports fixed grid dimensions (like FlashAttention-3).</p> <p><strong>FULL_DECODE_ONLY</strong> captures a full graph only for decode steps and runs prefill without any graph. This is ideal for Prefill/Decode disaggregated deployments where the decode instance never needs to handle prefill.</p> <p><strong>FULL_AND_PIECEWISE</strong> is the <strong>default in vLLM v1</strong>. It uses full graph capture for decode (where batch size is the only variable and sequence length is always 1) and piecewise capture for prefill (where sequence lengths vary). This combination works with most attention backends and delivers the best overall performance.</p>]]></content><author><name></name></author><category term="infrastructure"/><category term="cuda"/><category term="gpu"/><category term="vllm"/><category term="llm"/><category term="inference"/><category term="optimization"/><summary type="html"><![CDATA[How CUDA Graph reduces CPU launch overhead in LLM decode, memory management with Private Pools, and vLLM's graph capture modes]]></summary></entry><entry xml:lang="en"><title type="html">Multi-Node P/D Disagg vLLM Serving: How EFA Works Compared to InfiniBand?</title><link href="https://nsd9696.github.io/blog/2026/multi-node-gpu-networking-efa-srd-en/" rel="alternate" type="text/html" title="Multi-Node P/D Disagg vLLM Serving: How EFA Works Compared to InfiniBand?"/><published>2026-02-22T00:00:00+00:00</published><updated>2026-02-22T00:00:00+00:00</updated><id>https://nsd9696.github.io/blog/2026/multi-node-gpu-networking-efa-srd-en</id><content type="html" xml:base="https://nsd9696.github.io/blog/2026/multi-node-gpu-networking-efa-srd-en/"><![CDATA[<h2 id="1-multi-node-gpu-requirements">1. Multi-Node GPU Requirements</h2> <p>When GPUs communicate within a single node, NVLink provides hundreds of GB/s of bandwidth. NVLink enables direct point-to-point connections between GPUs, allowing them to communicate without going through the CPU.</p> <p>The problem arises when you go multi-node — the bottleneck shifts to the inter-node network. This is especially critical for AllReduce in Tensor Parallelism during LLM serving and KV cache transfer in Disaggregated Serving (Prefill/Decode separation). Naturally, TCP/IP can’t meet these demands.</p> <h3 id="the-limitations-of-tcpip">The Limitations of TCP/IP</h3> <p>TCP requires every packet to traverse the kernel network stack. This triggers a chain of system calls, context switches, protocol processing, and buffer copies, making it extremely slow. Unnecessary memcopy operations also occur in the process.</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>TCP/IP Path (Traditional):
GPU → cudaMemcpy → CPU RAM → send() → Kernel → NIC
  → Network →
NIC → Kernel → recv() → CPU RAM → cudaMemcpy → GPU

RDMA + GPUDirect Path:
GPU HBM → NIC (Direct DMA from GPU memory)
  → Network →
NIC → GPU HBM (Direct DMA)
</code></pre></div></div> <p>RDMA (Remote Direct Memory Access) bypasses the CPU and kernel, allowing the NIC to directly access memory. OS Bypass eliminates system call and context switch overhead. GPUDirect RDMA takes this a step further by enabling the NIC to perform DMA directly to GPU memory (HBM).</p> <h3 id="infiniband">InfiniBand</h3> <p>InfiniBand is what made GPUDirect RDMA possible. With each generation — HDR (A100 standard) and NDR (H100 standard) — per-port bandwidth roughly doubles (200Gbps → 400Gbps).</p> <hr/> <h2 id="2-efa-and-srd">2. EFA and SRD</h2> <h3 id="efa-elastic-fabric-adapter">EFA (Elastic Fabric Adapter)</h3> <p>EFA (Elastic Fabric Adapter) is a high-performance network interface designed by AWS — think of it as something similar to InfiniBand. It’s available on specific instance types (p4d, p5, p6, etc.) and provides the OS bypass and RDMA capabilities described above.</p> <h3 id="ethernet-vs-infiniband">Ethernet vs InfiniBand</h3> <p>The biggest difference with EFA is that it operates on standard Ethernet fabric. InfiniBand is a single-vendor ecosystem from NVIDIA (Mellanox) — switches, NICs, and cables all need to be Mellanox products, with virtually no alternatives. Ethernet equipment, on the other hand, is available from multiple vendors like Broadcom and Intel, making it commodity hardware.</p> <h3 id="rdma-transport-modes">RDMA Transport Modes</h3> <table> <thead> <tr> <th>Mode</th> <th>Dedicated Connection</th> <th>Delivery Guarantee</th> <th>Order Guarantee</th> <th>Description</th> </tr> </thead> <tbody> <tr> <td><strong>RC</strong> (Reliable Connection)</td> <td>Required (dedicated QP per peer)</td> <td>O</td> <td>O</td> <td>Default InfiniBand transport</td> </tr> <tr> <td><strong>UD</strong> (Unreliable Datagram)</td> <td>Not required (single QP)</td> <td>X</td> <td>X</td> <td>Lightest weight</td> </tr> <tr> <td><strong>RD</strong> (Reliable Datagram)</td> <td>Not required (single QP)</td> <td>O</td> <td>O</td> <td>Theoretically ideal but unimplemented</td> </tr> <tr> <td><strong>SRD</strong> (Scalable Reliable Datagram)</td> <td>Not required</td> <td>O</td> <td>X (SW reordering)</td> <td>Designed by AWS</td> </tr> </tbody> </table> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>RC (Reliable Connection):
Server A ──dedicated link──→ Server B
Server A ──dedicated link──→ Server C
→ Requires a dedicated QP (Queue Pair) per peer

UD (Unreliable Datagram):
Server A ──link──→ can send to anyone
→ No dedicated link needed, one QP for all peers

RD (Reliable Datagram):
Server A ──link──→ can send to anyone
→ No dedicated link + delivery guarantee + order guarantee
</code></pre></div></div> <h3 id="why-rd-is-not-actually-used">Why RD Is Not Actually Used</h3> <p>In practice, InfiniBand doesn’t really use RD. The reason comes down to hardware implementation complexity.</p> <ul> <li><strong>RC</strong> only needs to track state for one peer per QP, and <strong>UD</strong> doesn’t need state tracking at all since it’s fire-and-forget.</li> <li>But with <strong>RD</strong>, the NIC must track state for every communication peer: <ul> <li>“Sent up to packet 3 to Server B, received ACK up to 2”</li> <li>“Sent up to packet 7 to Server C, received ACK up to 5”</li> <li>“Sent packet 1 to Server D, no ACK yet”</li> <li>“Sent up to packet 12 to Server E, received ACK up to 10”</li> <li>… × every communication peer</li> </ul> </li> <li>For example, if a single QP communicates with 1,000 servers, the NIC must manage 1,000 states. These need to be stored in the NIC chip’s SRAM, which is expensive and small.</li> <li>So from NVIDIA’s perspective, RC and UD were sufficient, there was no demand for RD, and the cost-benefit didn’t make sense — so it was deprecated.</li> </ul> <h3 id="srd-scalable-reliable-datagram">SRD (Scalable Reliable Datagram)</h3> <p>So what’s different about AWS’s SRD? <strong>It gives up the ordering guarantee from RD.</strong></p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>RD:   Packet 1 → Packet 2 → Packet 3  (must arrive in order)
      Path: A ━━━━━━━━━━━━━→ B  (single path)

SRD:  Packet 1 ──Path A──→ ┐
      Packet 2 ──Path B──→ ├→ Software reorders after arrival
      Packet 3 ──Path C──→ ┘
</code></pre></div></div> <p>By giving up ordering, packets can be sprayed across multiple paths. If one path is congested, packets can take another — this is advantageous in Ethernet environments. In environments where thousands of servers share the network, SRD provides stability, which is why it was adopted.</p> <hr/> <h2 id="3-efa--gpudirect-rdma">3. EFA → GPUDirect RDMA</h2> <p>When using EFA, the GPUDirect RDMA flow looks like this:</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/efa_blog_1-480.webp 480w,/assets/img/efa_blog_1-800.webp 800w,/assets/img/efa_blog_1-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/efa_blog_1.png" class="img-fluid rounded z-depth-1 d-block mx-auto" width="80%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">EFA GPUDirect RDMA Flow</figcaption> </figure> <p>Of course, intra-node GPU communication still uses NVLink.</p> <h3 id="infiniband-vs-efa-performance-comparison">InfiniBand vs EFA Performance Comparison</h3> <p>So does EFA actually deliver comparable performance to InfiniBand? Here’s a summary of benchmark comparisons:</p> <table> <thead> <tr> <th>Metric</th> <th>InfiniBand</th> <th>EFA/Ethernet</th> <th>Conclusion</th> </tr> </thead> <tbody> <tr> <td><strong>Small message latency</strong></td> <td>~1 µs</td> <td>~10 µs</td> <td>IB dominant</td> </tr> <tr> <td><strong>Large transfer bandwidth</strong></td> <td>~200 Gbps</td> <td>~200 Gbps</td> <td>Similar</td> </tr> <tr> <td><strong>AI Training (large-scale)</strong></td> <td>Baseline</td> <td>Similar with proper tuning</td> <td>Minimal gap</td> </tr> <tr> <td><strong>AI Inference (Decode)</strong></td> <td>Favorable</td> <td>Avg 1.0166% slower</td> <td>IB slightly better</td> </tr> <tr> <td><strong>Cost</strong></td> <td>1.5–2.5x more expensive</td> <td>Baseline</td> <td>Ethernet wins</td> </tr> </tbody> </table> <blockquote> <p>Sources: <a href="https://www.wwt.com/blog/the-battle-of-ai-networking-ethernet-vs-infiniband">WWT - The Battle of AI Networking</a>, <a href="https://www.vitextech.com/blogs/blog/infiniband-vs-ethernet-for-ai-clusters-effective-gpu-networks-in-2025">Vitex Tech - InfiniBand vs Ethernet</a></p> </blockquote> <p>Overall, InfiniBand appears to have better raw performance. However, if you already have your setup on AWS, EFA can be advantageous. That said, considering that recent neo-cloud GPU pricing is significantly cheaper compared to AWS, InfiniBand might be the better choice.</p> <hr/> <h2 id="4-pd-disagg-vllm-serving-on-efa">4. P/D Disagg vLLM Serving on EFA</h2> <p>In my case, I was working on setting up Prefill/Decode Disagg in an A100 environment. In this scenario, EFA is directly involved in the kv_transfer stage.</p> <h3 id="kv-cache-transfer-software-stack">KV Cache Transfer Software Stack</h3> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/efa_blog_2-480.webp 480w,/assets/img/efa_blog_2-800.webp 800w,/assets/img/efa_blog_2-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/efa_blog_2.png" class="img-fluid rounded z-depth-1 d-block mx-auto" width="80%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">KV Cache Transfer Software Stack on EFA</figcaption> </figure> <p>Let’s look at each component above EFA in detail.</p> <h3 id="nixl-nvidia-inference-xfer-library">NIXL (NVIDIA Inference Xfer Library)</h3> <p>NIXL is a dedicated library for GPU-to-GPU memory transfer. The existing NCCL is optimized for collective communication like AllReduce and All-to-All, but it’s not well-suited for point-to-point transfers of memory blocks from one specific GPU to another — that’s why NIXL was created.</p> <p>It’s used in P/D Disagg for the <code class="language-plaintext highlighter-rouge">Prefill GPU ──RDMA Write──▶ Decode GPU</code> process, and for migration between Decode instances: <code class="language-plaintext highlighter-rouge">Decode GPU (Server A, overloaded) ──RDMA──▶ Decode GPU (Server B, available)</code>.</p> <p>NIXL includes a NIXL Agent that handles memory registration, metadata management (GPU memory addresses, RDMA keys, NIC addresses, etc.), and plugin backends (UCX, libfabric) that perform the actual transfers.</p> <h3 id="ucx-unified-communication-x">UCX (Unified Communication X)</h3> <p>UCX is a general-purpose communication framework originally built for InfiniBand. It internally supports protocols including RC, UD, TCP, cuda_ipc (for GPUs on the same node), and cuda_copy (GPU ↔ CPU copy).</p> <p>On EFA, you can use UCX by setting <code class="language-plaintext highlighter-rouge">UCX_TLS=ib</code>, since EFA provides an ibverbs-compatible interface.</p> <ul> <li><a href="https://github.com/amzn/amzn-drivers/blob/master/kernel/linux/efa/src/efa_verbs.c">EFA ibverbs implementation (AWS driver)</a></li> <li><a href="https://github.com/linux-rdma/rdma-core/blob/master/providers/efa/efa.c">rdma-core EFA provider</a></li> </ul> <h3 id="libfabric-open-fabrics-interface">libfabric (Open Fabrics Interface)</h3> <p>libfabric is the standard abstraction layer for network hardware vendors. It’s composed internally of providers including EFA provider, TCP provider, and SHM provider.</p> <p>Officially, libfabric appears to be the recommended path, but in practice UCX had better dependency compatibility. With libfabric, I kept running into GPU memory bad address issues, so I ended up deprecating it for my use case.</p> <h3 id="aws-ofi-nccl-optional">aws-ofi-nccl (optional)</h3> <p>Since my setup was P/D Disagg, I didn’t need NCCL. However, if your model is large enough to require multi-node setup, or for multi-node training, NCCL is necessary — and in EFA environments, aws-ofi-nccl is required.</p> <p>NCCL was originally designed for InfiniBand and has a built-in <code class="language-plaintext highlighter-rouge">net_ib</code> transport that directly calls IB Verbs APIs. aws-ofi-nccl implements NCCL’s network plugin API and translates it to libfabric’s RDM interface.</p> <h3 id="efa-nv-peermem">efa-nv-peermem</h3> <p>This is a kernel module that enables the NIC to directly access GPU memory. The standard version is nvidia-peermem, but for EFA, efa-nv-peermem is used instead.</p> <p>By default, NICs can only read CPU memory. efa-nv-peermem bridges the GPU and NIC, enabling direct access. (It’s a GPUDirect RDMA module.)</p>]]></content><author><name></name></author><category term="infrastructure"/><category term="gpu"/><category term="networking"/><category term="efa"/><category term="infiniband"/><category term="rdma"/><category term="vllm"/><summary type="html"><![CDATA[Multi-node GPU communication on AWS EFA, InfiniBand vs EFA comparison, and vLLM P/D Disagg setup]]></summary></entry><entry xml:lang="en"><title type="html">MoE Expert FFN Backend: experts_implementation</title><link href="https://nsd9696.github.io/blog/2026/moe-experts-implementation-backend-en/" rel="alternate" type="text/html" title="MoE Expert FFN Backend: experts_implementation"/><published>2026-01-30T10:00:00+00:00</published><updated>2026-01-30T10:00:00+00:00</updated><id>https://nsd9696.github.io/blog/2026/moe-experts-implementation-backend-en</id><content type="html" xml:base="https://nsd9696.github.io/blog/2026/moe-experts-implementation-backend-en/"><![CDATA[<h2 id="1-experts_implementation">1. experts_implementation</h2> <p>A <a href="https://github.com/huggingface/transformers/pull/42697">PR (#42697)</a> that adds support for selecting the Expert FFN computation method in MoE models has been merged into HuggingFace Transformers. Just like how <code class="language-plaintext highlighter-rouge">attn_implementation</code> allowed you to choose the attention computation backend, you can now hook into the expert computation and run it with the backend of your choice.</p> <hr/> <h2 id="2-eager-batched_mm-grouped_mm">2. eager, batched_mm, grouped_mm</h2> <p>Fundamentally, expert FFN follows the same logic: the router selects top-k experts, performs hidden state projection with expert parameters (<code class="language-plaintext highlighter-rouge">gate_up_proj</code>, <code class="language-plaintext highlighter-rouge">down_proj</code>), and then computes a weighted sum using routing weights. The key difference lies in <strong>how the per-expert matrix multiplications are performed</strong>.</p> <h3 id="eager-loop-based-reference-implementation">eager: Loop-Based Reference Implementation</h3> <p>This is the most intuitive approach. It iterates through activated experts one by one using a Python loop, selects only the tokens routed to each expert, and performs per-expert projection on those tokens. However, because it uses <code class="language-plaintext highlighter-rouge">torch.where</code> to select tokens assigned to each expert, it becomes difficult to use with <code class="language-plaintext highlighter-rouge">torch.compile</code> with the <code class="language-plaintext highlighter-rouge">fullgraph=True</code> option.</p> <h3 id="batched_mm">batched_mm</h3> <p><code class="language-plaintext highlighter-rouge">batched_mm</code> duplicates the selected expert’s weights for each token, stacks them into a 3D tensor, and performs batched matrix multiplication all at once using <code class="language-plaintext highlighter-rouge">torch.bmm</code>.</p> <p><code class="language-plaintext highlighter-rouge">torch.bmm</code> stands for Batched Matrix Multiplication — it multiplies pairs of identically-sized matrices all at once.</p> <p>Since <code class="language-plaintext highlighter-rouge">batched_mm</code> is compatible with <code class="language-plaintext highlighter-rouge">torch.compile</code>, it supports <code class="language-plaintext highlighter-rouge">fullgraph</code>. However, because it copies expert weights, memory usage can more than double compared to eager — making it more advantageous for short sequences or small batch sizes.</p> <h3 id="grouped_mm">grouped_mm</h3> <p><code class="language-plaintext highlighter-rouge">grouped_mm</code> uses <code class="language-plaintext highlighter-rouge">torch._grouped_mm</code> to support Grouped GEMM. This approach does not copy weights. Instead, it groups tokens by expert and processes all expert projections simultaneously using the Grouped GEMM kernel.</p> <p>Since weights are not duplicated, this approach is the most memory-efficient, and it particularly excels with long sequences and large batches.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/experts_impl_blog_1-480.webp 480w,/assets/img/experts_impl_blog_1-800.webp 800w,/assets/img/experts_impl_blog_1-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/experts_impl_blog_1.png" class="img-fluid rounded z-depth-1 d-block mx-auto" width="80%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Batched Matrix Multiplication (bmm) vs Grouped GEMM Approach</figcaption> </figure> <hr/> <h2 id="3-solar-open-100b-benchmark">3. Solar-Open 100B Benchmark</h2> <p>Since the PR showed significant performance differences, I ran benchmarks directly using Upstage’s Solar-Open model. Looking at just the Mean Latency, the differences were meaningful. <code class="language-plaintext highlighter-rouge">batched_mm</code> performed reasonably well with short, small inputs, but overall <code class="language-plaintext highlighter-rouge">grouped_mm</code> showed the best performance. Compared to eager, even without compile it was about 4x faster on average, and with compile applied the latency difference reached up to 10x.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/solar_latency_summary-480.webp 480w,/assets/img/solar_latency_summary-800.webp 800w,/assets/img/solar_latency_summary-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/solar_latency_summary.png" class="img-fluid rounded z-depth-1 d-block mx-auto" width="80%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Solar-Open 100B Latency Comparison (Experts Backend &amp; Torch Compile)</figcaption> </figure> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">model</span> <span class="o">=</span> <span class="n">AutoModelForCausalLM</span><span class="p">.</span><span class="nf">from_pretrained</span><span class="p">(</span>
    <span class="sh">"</span><span class="s">upstage/solar-open-...</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">experts_implementation</span><span class="o">=</span><span class="sh">"</span><span class="s">grouped_mm</span><span class="sh">"</span><span class="p">,</span>  <span class="c1"># or "batched_mm", "eager"
</span><span class="p">)</span>
</code></pre></div></div> <p>Note that with <code class="language-plaintext highlighter-rouge">batched_mm</code> under <code class="language-plaintext highlighter-rouge">batch_size=4</code>, <code class="language-plaintext highlighter-rouge">seq_len=128</code> conditions, a memory spike was observed during computation in both compile default and no-compile cases. I left the results as-is without separate modifications for reference.</p> <hr/> <h2 id="4-wrapping-up">4. Wrapping Up</h2> <p>It would be worth considering these backend options for MoE model inference. However, since vLLM has its own <code class="language-plaintext highlighter-rouge">fused_moe</code> kernel, it cannot be directly used there. I’ll cover the <code class="language-plaintext highlighter-rouge">fused_moe</code> kernel in a future post.</p>]]></content><author><name></name></author><category term="ml-engineering"/><category term="moe"/><category term="experts-implementation"/><category term="huggingface"/><category term="transformers"/><category term="torch-compile"/><category term="grouped-gemm"/><summary type="html"><![CDATA[Selecting Expert FFN computation backends (eager, batched_mm, grouped_mm) in HuggingFace Transformers and benchmarking with Solar-Open 100B]]></summary></entry></feed>